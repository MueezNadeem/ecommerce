<h1>Edit Product Detail</h1>

<%= form_for @prod, :url =>{:action => "update", :id =>@prod.id} do |f| %>

<p>Name: <%= f.text_field 'productName' %></p>
<%= f.submit "Save changes" %>
<% end %>

<%= link_to 'Back', {:action => 'list' } %>
